# STATE.md — current checkpoint (OVERWRITE every session-end; never append)
Updated: 2026-06-27 18:15 · Workstream: BACKEND · HEAD: (stage-7-eval; verify w/ git)
Current stage: 7 — Offline eval harness + Option-A routing + confidence refactor · Status: ✅ Complete (PM-verified)
Resume at: **Stage 8 — Anti-leakage & packaging hardening + `/security-review`** (`LEAK1`–`LEAK-S`, `PKG1`–`PKG3`, `SEC1`–`SEC2`). HALTED here per Asaf's "halt at the Stage 7 boundary." When resuming: prove all seven `LEAK*` cross-checks; packaging hardening — **make the Makefile robust without an active venv** (use `.venv/bin/python`/`.venv/bin/pytest` or document; the `make demo`/`test`/`eval` targets currently need venv active — the recurring packaging note); `.gitignore` correctness; README + module docstrings; track one redacted sample export + audit; run the native **`/security-review`** utility as the governance gate.
Live-truth (re-verify, don't trust → FACTS.md is source of truth): offline suite 315 pass / 1 skip via `make test` (venv); eval recall/grounding/routing_accuracy 1.0 + calibration via `make eval`; Option A live (i2→compliance); confidence score VALUE unchanged (0.799/0.861/0.880); Recall@5 1.0. All §5.1 reason-codes + §9 queues synced. Tags: …→ stage-6-pipeline → fix-sec1(c69cd98) → stage-7-eval.
Open halts / decisions pending Asaf (surface at boundary): (1) eval harness at `app/eval/harness.py` (Asaf wrote `app/eval_harness.py` — rename if flat preferred); (2) Makefile needs venv active (Stage-8 packaging target); (3) all prior flags (SEC1-miss owned; Option A live) recorded.
Last 3 superseded decisions (tombstones): sensitivity export-limbo → Option A: internal/restricted → compliance queue (Asaf 2026-06-27).
Disk-vs-ledger watch: `make test`/`demo`/`eval` need venv (Stage-8 fix candidate); audit/+exports/ regenerate on make demo/eval (gitignored; Stage 8 tracks one redacted sample each); no open code follow-ups (confidence dup cleared).
