# STATE.md — current checkpoint (OVERWRITE every session-end; never append)
Updated: 2026-06-27 16:20 · Workstream: BACKEND · HEAD: (stage-5-export; verify w/ git)
Current stage: 5 — Audit log + export + hard boundary · Status: ✅ Complete (PM-verified)
Resume at: **Stage 6 — End-to-end pipeline + the two demo cases** (`PIPE1`–`PIPE2`, `DEMO1`–`DEMO2`, `RULE1`–`RULE2`). HALTED here per Asaf's "halt at the Stage 5 boundary for my verification." When resuming: implement `app/pipeline.py` (intake→retrieve→assemble→draft→score→route→status→audit→export; `RULE_SAFE_TERMINAL` — component failures become routed terminals + `ERROR_TERMINAL`, never uncaught), `scripts/run_demo.py` (`make demo`, mocked, both cases) + `scripts/run_live_draft.py` (`make demo-live`, gated). Materialize `ERROR_TERMINAL` (last §5.1 reason-code). Fold in the Stage-6 follow-ups: build the BM25 index once (efficiency); present case_confident i1/i2 as auto-drafts + i3 as in-set routing (Asaf-confirmed defense-in-depth). Add `demo`/`demo-live` Makefile targets. Graded contracts (pipeline orchestration, RULE_* coverage) → reviewer gate fires.
Live-truth (re-verify, don't trust → FACTS.md is source of truth): offline suite 232 pass / 1 skip (39+32+46+63+53; skip=DRAFT2 live-gated) via `make test` (venv); audit append-only+redacted; export APPROVED-only local Markdown+CSV; export.py has zero network primitives; sensitivity gate + byte-exact REVIEW_BANNER verified; import creates no audit/exports dir. NEW §9: `SENSITIVITY_HOLD`,`EXTERNAL_SEND_BLOCKED`; schema +sensitivities/+review_approved. Tags: …→ stage-5-export.
Open halts / decisions pending Asaf (surface at boundary): the additive schema fields + 2 reason-codes (flagged; synced config↔§9). No blocking decisions.
Last 3 superseded decisions (tombstones): none.
Disk-vs-ledger watch: `make test` needs venv; audit/ + exports/ removed during QA (gitignored, regenerate on first run — Stage 8 may track one redacted sample each); open follow-ups → S6: BM25-build-once + Makefile demo targets + ERROR_TERMINAL; S7: confidence-rationale dup.
