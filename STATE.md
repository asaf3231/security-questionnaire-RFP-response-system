# STATE.md — current checkpoint (OVERWRITE every session-end; never append)
Updated: 2026-06-27 17:30 · Workstream: BACKEND · HEAD: (stage-6-pipeline; verify w/ git)
Current stage: 6 — End-to-end pipeline + the two demo cases · Status: ✅ Complete (PM-verified)
Resume at: **Stage 7 — Offline evaluation harness** (`EVAL1`–`EVAL3`, `RET2`, `LEAK4`–`LEAK5`). HALTED here per Asaf's "halt at the Stage 6 boundary for my verification." When resuming: build `app/eval/harness.py` (deterministic eval over labeled held-out fixtures: Recall@K + grounding rate + routing accuracy + confidence calibration, all computed; `RULE_NO_EVAL_CONTAMINATION` held-out split proof; `RULE_NO_FABRICATED_METRIC`) + `make eval`; ALSO clear the S7 follow-up (confidence.py rationale recomputes coverage/dominance → refactor `_compute_score` to return components). Pure-eval stage → PM's own QA suffices (no reviewer gate unless it touches a graded contract).
Live-truth (re-verify, don't trust → FACTS.md is source of truth): offline suite 278 pass / 1 skip via `make test` (venv); Recall@5 held 1.0 post full-corpus Retriever refactor; safe-terminal verified (injected failure → no uncaught exception); DEMO1/DEMO2 behaviors verified; `make demo` clean offline (venv). All §5.1 reason-codes materialized (ERROR_TERMINAL last). Tags: …→ stage-5-export → fix-sec1(c69cd98) → stage-6-pipeline.
Open halts / decisions pending Asaf (surface at boundary): (1) **SEC1 was red since stage-5-export — earlier "232 green" wrong; fixed+owned** (NOTES FIX-SEC1); (2) **OPEN DESIGN QUESTION** — internal/restricted sensitivity = export gate but NOT a routing trigger → confident internal item (case_confident-i2) is "stuck" + dropped from demo summary; route-vs-gate decision needed (NOTES D-S6); (3) retrieval semantics now full-corpus IDF (Recall@K held 1.0); (4) `make demo`/`make test` need venv active (Stage-8 packaging).
Last 3 superseded decisions (tombstones): none.
Disk-vs-ledger watch: `make test`/`make demo` need venv; audit/+exports/ now populated by `make demo` (gitignored); spine compacted this session (archives present); S7 follow-up: confidence-rationale dup.
