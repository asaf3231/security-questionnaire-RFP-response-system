# STATE.md — current checkpoint (OVERWRITE every session-end; never append)
Updated: 2026-06-27 15:25 · Workstream: BACKEND · HEAD: (stage-4-routing; verify w/ git)
Current stage: 4 — Confidence + routing + state machine · Status: ✅ Complete (PM-verified)
Resume at: **Stage 5 — Audit log + export + hard boundary** (`AUDIT1`–`AUDIT3`, `EXPORT1`–`EXPORT3`, `BOUND1`–`BOUND2`). HALTED here per Asaf's "halt at the Stage 4 boundary for my review." When resuming: implement `app/audit.py` (append-only JSONL, one event/transition+tool call, `RULE_AUDIT_COMPLETE`, redaction), `app/export.py` (Markdown + CSV; only APPROVED items; `RULE_NO_EXTERNAL_SEND` + `RULE_SENSITIVITY_GATE` + byte-exact `REVIEW_BANNER`). Materialize §5.1 reason-codes Stage 5 needs (`SENSITIVITY_HOLD`, `EXTERNAL_SEND_BLOCKED`). Graded contracts → reviewer gate fires.
Live-truth (re-verify, don't trust → FACTS.md is source of truth): offline suite 179 pass / 1 skip (39+32+46+63; skip=DRAFT2 live-gated) via `make test` (venv); confidence number model-independent; 3 routing triggers + self-approve guard verified; real demo routing characterized (FACTS). NEW §9: `DEFAULT_REVIEWER_QUEUE`, `ROUTED_HIGH_RISK`/`ROUTED_AMBIGUOUS`/`ROUTED_LOW_CONFIDENCE`/`SELF_APPROVE_BLOCKED`. Tags: …→ stage-4-routing.
Open halts / decisions pending Asaf (surface at boundary): (1) the 5 NEW §9 constants (additions; synced config↔§9; `DEFAULT_REVIEWER_QUEUE="engineering"` retunable); (2) **Stage-6 demo nuance** — `case_confident-i3` routes via its security tag (decide: showcase i1/i2 vs retune i3); (3) deferred minor — confidence.py rationale recomputes coverage/dominance (fix at Stage 7).
Last 3 superseded decisions (tombstones): none.
Disk-vs-ledger watch: `make test` needs venv; Stage 6 BM25-rebuild + ambiguity-normalization + case_confident-i3 demo notes open; grounding lexical (known).
