# STATE.md — current checkpoint (OVERWRITE every session-end; never append)
Updated: 2026-06-27 19:10 · Workstream: BACKEND · HEAD: (honest stage-7-eval / 7r; verify w/ git)
Current stage: 7 — Eval harness + Option-A routing + confidence refactor · Status: ✅ Complete via HONEST re-do (7r)
Resume at: **Stage 8 — Anti-leakage & packaging hardening + `/security-review`** (`LEAK1`–`LEAK-S`, `PKG1`–`PKG3`). HALTED here for Asaf. When resuming: prove all 7 `LEAK*`; harden the Makefile so `make test`/`demo`/`eval` work without a manually-activated venv; `.gitignore` correctness; README + module docstrings; track one redacted sample export+audit; run the native **`/security-review`** governance gate.
Live-truth (re-verify, don't trust → FACTS.md is source of truth): offline suite 315 pass / 1 skip via `make test` (venv); HONEST eval via `make eval`: recall 1.0 / grounding match 1.0 / raw_grounded 0.833 / routing_acc 1.0 / calibration review{ungrounded=1}; **eval-006 honestly caught** (grounded=False, score 0.074, ROUTED_LOW_CONFIDENCE) — PM ran the real pipeline; perturb-proof confirms computed metrics. Demo items unchanged. Tags: stage-0-spine … stage-6-pipeline, then the honest stage-7-eval.
Open halts / decisions pending Asaf (surface at boundary): graded changes in the 7r fix — confidence single-chunk-dominance fix; **NEW** grounding question-relevance (`grounding_check(question=...)` + `GROUNDING_QUESTION_COVERAGE_MIN=0.30` §9) closing the lexical-grounding limitation; eval-006 gold reverted. Also still open: eval path `app/eval/harness.py` (vs Asaf's `app/eval_harness.py`); Makefile-needs-venv (Stage-8).
Last 3 superseded decisions (tombstones): Stage-7 fabricated eval (39469d0) → REJECTED → honest 7r fix (2026-06-27); sensitivity export-limbo → Option A compliance routing (2026-06-27).
Disk-vs-ledger watch: commit 39469d0 (tag previously stage-7-eval) is the REJECTED fabricated attempt — kept in history; the honest fix is a later commit and `stage-7-eval` is re-pointed to it. `make test`/`eval` need venv (Stage-8 fix).
