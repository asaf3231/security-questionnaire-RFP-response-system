# STATE.md — current checkpoint (OVERWRITE every session-end; never append)
Updated: 2026-06-28 13:15 · Workstream: BACKEND/QA · HEAD: a68439b (submission cleanup) · branch redteam/crazy-testing (pushed to public origin)
Current stage: **Post-build QA hardening (DN-QA50) + submission prep.** Stages 0–10 ✅ + Stage 9 Brief/Appendix ✅ (brief/REINDEER_BRIEF.md, appendix/TECHNICAL_APPENDIX.md). All planned build stages complete.
Resume at: Asaf to **(1)** pin PR-3 `N` (sweep ⇒ `GROUNDING_MAX_UNGROUNDED_TOKENS=50`) + PR-4 band (sweep ⇒ `[0.50,0.60)`, NOT the audit's [0.45,0.55) which misses BO-026) — both DEFERRED pending his pin; **(2)** decide the history-scrub (Reference/ + home-path remain in git HISTORY) + merge-to-main; **(3)** optionally run native `/security-review` (origin now exists).
Live-truth (re-verify, don't trust → FACTS.md is source of truth): suite **580 pass / 1 skip / 2 xfail** via `make test` (= 569 + 11 DN-QA50 PR-1/PR-2; existing 569 byte-identical). `make eval` **byte-identical** (recall 1.0 / raw_grounded 0.833 / routing 1.0 / review{u1}). `make demo` unchanged (i1 0.799→exported, i2 0.861→compliance, i3 0.880→security, case_review→legal).
Open halts / decisions pending Asaf: **(a)** PR-3 N + PR-4 band pin (PR-1/PR-2 already landed `1f789cf` + pushed); **(b)** history-scrub of Reference/ home-path (HEAD clean, history not) + force-push?; **(c)** merge `redteam/crazy-testing` → `main` for submission?; **(d)** optional doc-consistency sweep (CLAUDE §2 / PLAN refer to "stages 0–9"; PM_LOG drift).
Last 3 superseded decisions (tombstones): "no `origin` remote" → **origin EXISTS** (public `asaf3231/security-questionnaire-RFP-response-system`, native /security-review now runnable) (2026-06-28); STATE HEAD 45c0dc6 → a68439b after cleaner/open-remote/DN-QA50/cleanup (2026-06-28); Reference/+HANDOFF+crazy_test_prompt+case_bulk20 **untracked** for submission (kept local, gitignored) (2026-06-28).
Disk-vs-ledger watch: **IN-FLIGHT uncommitted (concurrent work):** auto-tagging at intake (`AUTO_TAG_MAX`) modifies `app/config.py` + `app/pipeline.py` — NOT yet a committed/verified stage; reconcile before next commit. 5 redteam live-logs + Reference/ + HANDOFF + crazy_test_prompt + case_bulk20 untracked+gitignored (local copies kept). DN-QA50 recorded in NOTES.md (DN-QA50 block + VERIFIER VERDICT); sweep in redteam/SWEEP_PR3_PR4.md; audit in redteam/QA_AUDIT_50.md.
