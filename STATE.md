# STATE.md — current checkpoint (OVERWRITE every session-end; never append)
Updated: 2026-06-27 14:35 · Workstream: BACKEND · HEAD: (stage-3-draft; verify w/ git)
Current stage: 3 — Context Stack + draft generation + grounding · Status: ✅ Complete (PM-verified)
Resume at: **Stage 4 — Confidence + routing + state machine** (`CONF1`–`CONF3`, `ROUTE1`–`ROUTE3`, `STATUS1`–`STATUS2`). HALTED here per Asaf's "halt at the Stage 3 boundary for my review." When resuming: implement `app/confidence.py` (hybrid: deterministic property-validator score from retrieval top-score/coverage/grounding + LLM rationale only), `app/routing.py` (the 3 `RULE_HITM_REVIEW_TRIGGER` triggers → reviewer queue from policy_tags map), `app/state.py` (ITEM_STATES machine + `RULE_NO_SELF_APPROVE` guard). Materialize the §5.1 reason-codes Stage 4 needs (`ROUTED_HIGH_RISK`/`ROUTED_AMBIGUOUS`/`ROUTED_LOW_CONFIDENCE`/`SELF_APPROVE_BLOCKED`). Graded contracts → reviewer gate fires.
Live-truth (re-verify, don't trust → FACTS.md is source of truth): offline suite 116 pass / 1 skip (39 S1 + 32 S2 + 46 S3; skip = DRAFT2 live-gated) via `make test` (venv); grounding gate byte-exact + 3 conditions verified; MockLLM deterministic; ClaudeLLM lazy/gated/degrades. NEW §9 constants: `GROUNDING_COVERAGE_MIN=0.5`, `GROUNDING_FAIL`. Tags: stage-0-spine → stage-1-env → stage-2-retrieval → stage-3-draft.
Open halts / decisions pending Asaf (surface at boundary): the two NEW §9 constants added in Stage 3 (`GROUNDING_COVERAGE_MIN`, `GROUNDING_FAIL`) — additions intrinsic to the grounding gate Asaf requested; synced config↔§9; flagged for review (adjust value/approach if desired). Known limitation: grounding is lexical, not semantic.
Last 3 superseded decisions (tombstones): none.
Disk-vs-ledger watch: `make test` needs venv; Stage 6 efficiency follow-up open (BM25 rebuilt per retrieve()); §5.1 reason-codes materialized incrementally (only `GROUNDING_FAIL` so far).
