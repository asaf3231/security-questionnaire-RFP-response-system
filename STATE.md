# STATE.md — current checkpoint (OVERWRITE every session-end; never append)
Updated: 2026-06-27 16:40 · Workstream: BACKEND · HEAD: 8924057 (stage-5-export; verify w/ git)
Current stage: 6 — End-to-end pipeline + the two demo cases · Status: 🔄 In progress (autonomous loop)
Resume at: spawn / await the cold executer on `briefs/stage-6.md`; on return PM re-runs `PIPE1`–`PIPE2`, `DEMO1`–`DEMO2`, `RULE1`–`RULE2` + `make test` + `make demo` itself, re-verifies Recall@K (retrieval refactor changes scores → full-corpus IDF), the two demo behaviors (i1/i2 auto→approved→exported; case_review + i3 routed), `RULE_SAFE_TERMINAL`/`ERROR_TERMINAL` on injected failure (no uncaught exception), every `RULE_*` reason-code present in the audit, + verifier-independence (RET1–3 NOT weakened), then `/code-review`. Record FACTS, then HALT at the Stage 6 boundary for Asaf.
Live-truth (re-verify, don't trust → FACTS.md is source of truth): offline suite 232 pass / 1 skip via `make test` (venv); Recall@5=1.0 (WILL be re-verified post-refactor); all 14 modules import-safe. Tags: …→ stage-5-export.
Open halts / decisions pending Asaf (surface at boundary): (1) **retrieval semantics change** — build-once full-corpus BM25 index + post-filter (Asaf req #2); shifts scores vs Stage-2 filtered-corpus; RET1–3 must stay green + Recall@K ≥ target (executer halts DECISION-NEEDED if not — no test-weakening); (2) ADD `ERROR_TERMINAL` reason-code (last §5.1) + the `Retriever` class API. Additive/flagged.
Last 3 superseded decisions (tombstones): none.
Disk-vs-ledger watch: `make test` needs venv; audit/+exports/ regenerate on `make demo`; remaining follow-up after S6 → S7 confidence-rationale dup.
